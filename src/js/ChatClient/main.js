var React = require('react');
var ReactDOM = require('react-dom');
var ChatClient = require('./components/chat-client');

ReactDOM.render(<ChatClient/>, document.getElementById('react-container'));
